# .NET
bin/
obj/
*.user
.vs/
.idea/

# Local secrets — never commit real keys/connection strings
appsettings.Local.json
*.env

# Legacy Firebase archive keeps its own ignores
legacy-firebase/functions/node_modules/
legacy-firebase/functions/lib/
legacy-firebase/.firebase/
